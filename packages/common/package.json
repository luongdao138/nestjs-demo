{
  "name": "@piom/common",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "watch": "tsc -p ./tsconfig.build.json -w",
    "lint": "eslint --fix .",
    "build": "rimraf dist && tsc -p ./tsconfig.build.json"
  },
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "typescript": "^5.1.6",
    "rimraf": "^5.0.1"
  }
}
